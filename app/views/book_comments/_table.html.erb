<table>
  <% book_comments.each do |comment| %>
    <tr>
      <td><%= image_tag comment.user.get_profile_image(100,100) %></td>
      <td><%= comment.body %></td>
      <% if comment.user_id == current_user.id %>
      <td><%= link_to "削除", book_book_comment_path(book,comment), method: :delete, class: "btn btn-danger", remote: true %></td>
      
      <% else %>
      <td><%= link_to '削除' %></td>
      <% end %>
    </tr>
  <% end %>
</table>